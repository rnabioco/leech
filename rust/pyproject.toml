[build-system]
requires = ["maturin>=1.12.6"]
build-backend = "maturin"

[project]
name = "leech-core"
# Single source is rust/Cargo.toml; maturin reads it through this.
dynamic = ["version"]
requires-python = ">=3.12"

[tool.maturin]
features = ["pyo3/extension-module"]
module-name = "leech_core"
