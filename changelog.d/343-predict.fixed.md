- **`leech predict`'s Rust path no longer misaligns `signal_kmer` chunks through a centre-cropped `--signal-context-bases` window.** `rust/src/inference_pipeline/inference.rs` relies entirely on `escapepod_signal::chunk::cut_chunk`'s internal `SignalKmer` arm, which carried the same pre-crop-vs-post-crop mismatch fixed in `data prepare`'s Rust and Python paths (#343): `place_window` centre-crops a requested window wider than `--signal-len`, but the k-mer map was still built from the raw, pre-crop origin. Fixed upstream in `escapepod-signal` v0.27.1 (rnabioco/escapepod-rs#388); this pins `rust/Cargo.toml` to that release, so predict inherits the fix with no code change of its own. A new Rust unit test exercises the exact per-chunk wiring `process_one_read` uses against a hand-verified fixture, closing the last piece of #343.
